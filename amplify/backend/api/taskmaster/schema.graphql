type Task @model
@key(name: "byTask", fields: ["teamID", "title", "body", "state"]){
  id: ID!
  teamID: ID!
  title: String
  body: String
  state: String
}
type Team @model {
  id: ID!
  name: String!
  tasks: [Tasks] @connection(keyName: "byTask", fields: ["id"])
}